name: Docker Image CI

on:
  push:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Docker Login
      run: |
        docker login -u ${{DOCKER_USERNAME}} -p ${{DOCKER_PASSWORD}}
    - name: Build the Docker image
      run: docker build -t saenthan/authenticationservice .
